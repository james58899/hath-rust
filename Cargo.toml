[package]
name = "hath-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-files = "0.6"
actix-tls = { version = "*", default-features = false, features = ["accept"] }
actix-web = { version = "4.3", default-features = false, features = ["macros", "openssl"] }
actix-web-lab = "0.19"
async-stream = "0.3"
chrono = "0.4"
clap = { version = "4.3", features = ["derive"] }
cpufeatures = "0.2"
filesize = "0.2"
filetime = "0.2"
futures = "0.3"
hex = "0.4"
log = "0.4"
mime = "0.3"
openssl = { version = "*", features = ["vendored"] }
parking_lot = { version = "0.12", features = ["hardware-lock-elision", "deadlock_detection"] }
pin-project-lite = "0.2"
rand = { version = "0.8", default-features = false, features = ["small_rng"] }
regex = "1.8"
reqwest = { version = "0.11", default-features = false, features = ["rustls-tls", "stream"] }
tempfile = "3.6"
tokio = { version = "1", features = ["full", "parking_lot"] }
tokio-stream = { version = "0.1", default-features = false, features = ["fs"] }

[target.'cfg(not(target_env = "msvc"))'.dependencies]
tikv-jemallocator = { version = "0.5.0", features = ["background_threads", "unprefixed_malloc_on_supported_platforms"] }

[target.'cfg(target_env = "msvc")'.dependencies]
mimalloc = { version = "0.1", default-features = false }

[build-dependencies]
openssl-src = { version = "111", features = ["weak-crypto"] }

[profile.release]
lto = true
strip = true
